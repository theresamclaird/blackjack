import React from 'react';
import Box from '../Box';

const Table = () => {
    return (
        <Box sx={{ bg: 'feltGreen', width: '100vw', height: '100vh' }}>
            
        </Box>
    );
};

export default Table;